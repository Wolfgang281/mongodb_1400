//! json --> 1) it is language independent
//? used for exchanging data
//? it lightweight(stores alpha-numeric data), human-readable.
//? stores data in key-value pair separated by comma, here keys are stored in double quotes.

//! do not use comma after last key-value pair

//~ in json we cannot pass undefined, functions and date

//! BSON --> binary json (mongodb)
